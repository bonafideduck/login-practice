{
  "name": "login-practice",
  "version": "1.0.0",
  "description": "A Vue.js application for practicing password manager usage",
  "packageManager": "pnpm@8.15.0",
  "engines": {
    "node": ">=16.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "preinstall": "node -e \"if(process.env.npm_execpath.indexOf('pnpm') === -1) throw new Error('Please use pnpm instead of npm')\"",
    "dev": "vite",
    "build": "NODE_ENV=production vite build",
    "preview": "vite preview",
    "deploy": "pnpm run build && gh-pages -d dist -b gh-pages-new"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "vue-router": "^4.2.5"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "gh-pages": "^6.1.0",
    "vite": "^5.0.0"
  }
}
